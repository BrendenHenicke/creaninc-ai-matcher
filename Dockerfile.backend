# Clean backend Dockerfile for Flask API
FROM python:3.11-slim

WORKDIR /app

# Copy only necessary files first (avoids copying venv, caches, etc.)
COPY requirements.txt requirements.txt

# Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application (excluding venv, logs, etc.)
COPY . .

# Expose Flask port
ENV PORT=5000
EXPOSE 5000

# Run backend
CMD ["python", "app.py"]
